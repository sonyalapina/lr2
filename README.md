# new features

1. language changed to English
2. multiple servers can now run simultaneously on the same system. each server generates a unique identifier. communication files are named with server-specific IDs
3. servers notify all connected clients before termination
